<% page_header "Groupes" %>

<div id="groups-list">
  <% if @groups.any? %>
    <%= render @groups %>
  <% else %>
    <p style="text-align: center; color: var(--text-muted); padding: 40px 0;">
      Aucun groupe pour le moment.
    </p>
  <% end %>
</div>

<div style="margin-top: 20px;">
  <%= link_to "Créer une PolyBanque", new_group_path, class: "button" %>
</div>