<div class="mb-4 text-center">
  <h3 class="text-muted">Historique</h3>
</div>

<div class="flex flex-col gap-1">
  <% if @operations.any? %>
    <%= render operations %>
  <% else %>
    <p class="text-muted text-center text-sm mt-4">Aucune dépense enregistrée.</p>
  <% end %>
</div>